```

Tested in node v8.4.0
1. npm i
2. to run the app: 
DB='myDbName' DB_USER='myDbUser' DB_PASS='myDbPass' SECRET_PHRASE='wtf?' npm run dev
3. to test the endpoint you can use POSTMAN and send the body as a raw object
```
